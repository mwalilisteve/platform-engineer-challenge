apiVersion: v1
kind: ConfigMap
metadata:
  name: api-config
  namespace: default
data:
  NODE_ENV: production
  LOG_LEVEL: info
  # ISSUE 4: This is sensitive — should be in a Secret, not a ConfigMap
